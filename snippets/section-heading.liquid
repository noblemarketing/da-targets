{%- if section.settings.button_heading != blank and section.settings.link_heading != blank -%}
  <div class="heading-flex d-flex flex-wrap align-items-center justify-content-between mb-30">
    <div class="heading-left">
      {%- if settings.sectionheading == 'heading-1' -%}
      {%- if section.settings.title_top != blank -%}
      <div
        class="subtop text-{{ section.settings.align-heading }} text-top {% if section.settings.title != blank %} mb-2 {% else %} mb-4 {% endif %}"
        data-aos="{{ section.settings.animation }}" data-aos-duration="300">{{ section.settings.title_top
        }}</div>
      {%- endif -%}
      {%- if section.settings.title != blank -%}
      <h3
        class="section-title-1 text-{{ section.settings.align-heading }}  {% if section.settings.des != blank %} mb-2 {% else %} mb-0 {% endif %}"
        data-aos="{{ section.settings.animation }}" data-aos-duration="500">
        <span>{{ section.settings.title }}</span>
      </h3>
      {%- endif -%}
      {%- if section.settings.des != blank -%}
      <div class="font-family-2 mb-0 text-{{ section.settings.align-heading }}"
        data-aos="{{ section.settings.animation }}" data-aos-duration="800">{{ section.settings.des }}
      </div>
      {%- endif -%}
      {%- elsif settings.sectionheading == 'heading-2' -%}
      {%- if section.settings.title_top != blank -%}
      <div class="subtop text-center text-top" data-aos="{{ section.settings.animation }}"
        data-aos-duration="300">{{ section.settings.title_top }}</div>
      {%- endif -%}
      {%- if section.settings.title != blank -%}
      <h3 class="section-title-2 {% if section.settings.des != blank %} mb-2 {% else %} mb-5 {% endif %}">
        <span>{{ section.settings.title }}</span>
      </h3>
      {%- endif -%}
      {%- if section.settings.des != blank -%}
      <p class="font-family-2 mb-5">{{ section.settings.des }}</p>
      {%- endif -%}
      {%- endif -%}
    </div>
    <div class="heading-right">
      <a href="{{ section.settings.link_heading }}" class="btn {{ section.settings.style_btn }}">
        {{ section.settings.button_heading }}
      </a>
    </div>
  </div>
{%- else -%}
  <!-- section-heading.liquid - Apply for all section title -->
  {%- if settings.sectionheading == 'heading-1' -%}
    {%- if section.settings.title_top != blank -%}
      <div class="subtop text-{{ section.settings.align-heading }} text-top {% if section.settings.title != blank %} mb-2 {% else %} mb-4 {% endif %}" data-aos="{{ section.settings.animation }}" data-aos-duration="300">{{ section.settings.title_top }}</div>
    {%- endif -%}
    {%- if section.settings.title != blank -%}
    <h3 class="section-title-1 text-{{ section.settings.align-heading }}  {% if section.settings.des != blank %} mb-20 {% else if section.settings.title_sp != blank %} mb-sm-50 mb-30 {% else %} mb-5 {% endif %}" data-aos="{{ section.settings.animation }}" data-aos-duration="500">
      <span>{{ section.settings.title }}</span>
      {% if section.settings.title_sp != blank %}
      <p>{{ section.settings.title_sp }}</p>
      {% endif %}
    </h3>
    {%- endif -%}
    {%- if section.settings.des != blank -%}
    <div class="font-family-2 mb-5 text-{{ section.settings.align-heading }}" data-aos="{{ section.settings.animation }}" data-aos-duration="800">{{ section.settings.des }}</div>
    {%- endif -%}
    {%- if section.settings.button_heading != blank -%}
      <div class="btn-heading text-{{ section.settings.align-heading }}">
        <a href="{{ section.settings.link_heading }}" class="btn {{ section.settings.style_btn }}">
          {{ section.settings.button_heading }}
        </a>
      </div>
    {%- endif -%}
  {%- elsif settings.sectionheading == 'heading-2' -%}
    {%- if section.settings.title_top != blank -%}
      <div class="subtop text-center text-top" data-aos="{{ section.settings.animation }}" data-aos-duration="300">{{ section.settings.title_top }}</div>
    {%- endif -%}
    {%- if section.settings.title != blank -%}
    <h3 class="section-title-2 {% if section.settings.des != blank %} mb-20 {% else %} mb-5 {% endif %}">
      <span>{{ section.settings.title }}</span>
    </h3>
    {%- endif -%}
    {%- if section.settings.des != blank -%}
    <p class="font-family-2 mb-5">{{ section.settings.des }}</p>
    {%- endif -%}
    {%- if section.settings.button_heading != blank -%}
      <div class="btn-heading text-{{ section.settings.align-heading }}">
        <a href="{{ section.settings.link_heading }}" class="btn {{ section.settings.style_btn }}">
          {{ section.settings.button_heading }}
        </a>
      </div>
    {%- endif -%}
  {%- endif -%}
{%- endif -%}